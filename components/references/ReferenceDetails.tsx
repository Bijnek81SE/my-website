import Link from "next/link";
import type { ChemistryReference } from "@/content/references";

export default function ReferenceDetails({ entry }: { entry: ChemistryReference }) {
  const isGroup = entry.kind === "functional-group";
  return <main className="bg-slate-50 py-12 sm:py-16"><article className="mx-auto max-w-5xl px-5 sm:px-6 lg:px-8"><Link href={isGroup ? "/functional-groups" : "/reagents"} className="font-semibold text-emerald-700">← Back to {isGroup ? "functional groups" : "reagents"}</Link><header className="mt-6 rounded-3xl bg-slate-950 p-8 text-white"><p className="text-sm font-semibold uppercase tracking-widest text-emerald-300">{entry.category}</p><div className="mt-3 flex flex-wrap items-end justify-between gap-4"><h1 className="text-4xl font-bold">{entry.name}</h1><p className="font-mono text-xl text-slate-300">{entry.formula}</p></div><p className="mt-4 max-w-3xl text-lg leading-8 text-slate-300">{entry.summary}</p></header>{isGroup ? <div className="mt-8 grid gap-6 md:grid-cols-2">{[["How to recognise it", entry.recognition], ["Bonding and geometry", entry.bonding], ["Polarity", entry.polarity], ["Acidity and basicity", entry.acidityBasicity]].map(([title, text]) => <section key={title} className="rounded-2xl border border-slate-200 bg-white p-6"><h2 className="text-xl font-bold text-slate-950">{title}</h2><p className="mt-3 leading-7 text-slate-600">{text}</p></section>)}<section className="rounded-2xl border border-slate-200 bg-white p-6 md:col-span-2"><h2 className="text-xl font-bold">Common reactions</h2><ul className="mt-3 grid gap-2 sm:grid-cols-2">{entry.commonReactions.map((item) => <li key={item} className="rounded-lg bg-slate-50 px-3 py-2">{item}</li>)}</ul></section></div> : <div className="mt-8 grid gap-6 md:grid-cols-2">{[["Purpose", entry.purpose], ["Selectivity", entry.selectivity], ["Safety", entry.safety]].map(([title, text]) => <section key={title} className="rounded-2xl border border-slate-200 bg-white p-6"><h2 className="text-xl font-bold">{title}</h2><p className="mt-3 leading-7 text-slate-600">{text}</p></section>)}<section className="rounded-2xl border border-slate-200 bg-white p-6"><h2 className="text-xl font-bold">Typical conditions</h2><ul className="mt-3 list-disc space-y-2 pl-5">{entry.conditions.map((item) => <li key={item}>{item}</li>)}</ul></section><section className="rounded-2xl border border-slate-200 bg-white p-6"><h2 className="text-xl font-bold">Limitations</h2><ul className="mt-3 list-disc space-y-2 pl-5">{entry.limitations.map((item) => <li key={item}>{item}</li>)}</ul></section><section className="rounded-2xl border border-slate-200 bg-white p-6"><h2 className="text-xl font-bold">Alternatives</h2><ul className="mt-3 list-disc space-y-2 pl-5">{entry.alternatives.map((item) => <li key={item}>{item}</li>)}</ul></section></div>}<section className="mt-8 rounded-2xl border border-emerald-200 bg-emerald-50 p-6"><h2 className="text-xl font-bold">Connected learning</h2><div className="mt-4 flex flex-wrap gap-3">{entry.relatedReactions.map((item) => <Link key={item.href} href={item.href} className="rounded-xl bg-emerald-700 px-4 py-2.5 font-semibold text-white">{item.label}</Link>)}{isGroup && entry.relatedLabs.map((item) => <Link key={item.href} href={item.href} className="rounded-xl border border-emerald-300 bg-white px-4 py-2.5 font-semibold text-emerald-800">{item.label}</Link>)}</div></section></article></main>;
}
