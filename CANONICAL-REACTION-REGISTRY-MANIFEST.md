# Canonical Reaction Registry — Milestone 3

This milestone moves the reaction catalog into individual typed records and adds indexed lookup, reusable selectors, relationship validation, and registry-driven Reaction Explorer filtering.

Future reactions are added as one record in `content/reactions/records/` plus one registry import.
