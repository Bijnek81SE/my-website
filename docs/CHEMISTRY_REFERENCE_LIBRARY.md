# Chemistry Reference Library

Reference entries live in `content/references/` and are rendered by shared components in `components/references/`.

Each entry must use a unique slug, concise evidence-based language, useful search keywords, and links to existing reaction or lab routes. Dynamic routes use registry data for static generation and metadata.
